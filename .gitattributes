# 文本文件统一用 LF，避免在 Windows / macOS 之间反复产生整文件差异
* text=auto eol=lf

*.cs       text diff=csharp
*.csproj   text
*.md       text
*.json     text
*.gitignore text
*.gitattributes text

# Unity 的 YAML 资源（场景、预制体、meta）
*.unity    text
*.prefab   text
*.asset    text
*.meta     text

# 二进制资源：交给 git 自己判断，不要做行尾转换
*.png      binary
*.jpg      binary
*.wav      binary
*.mp3      binary
*.ogg      binary
*.ttf      binary

# Unity 用 YAML 序列化，但 diff 出来很难读，这里让 GitHub 折叠显示
*.unity    linguist-generated
*.asset    linguist-generated

# ---------------------------------------------------------------
# Windows 批处理必须是 CRLF（放在文件末尾，靠"后面的规则优先"覆盖上面的 eol=lf）
#
# tools/build_apk.bat 是要双击运行的批处理。LF 行尾在多数情况下也能跑，
# 但一碰到多行块 / goto / label 就会出怪问题，所以强制 CRLF。
# 同理，批处理内容必须保持纯 ASCII —— 非 ASCII 会被 OEM 代码页搞乱，
# 连里面的路径都可能变花，所以 .bat 里的注释写英文。
# ---------------------------------------------------------------
*.bat      text eol=crlf
*.cmd      text eol=crlf
